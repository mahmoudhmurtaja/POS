@model IEnumerable<POS_2._0.Web.Models.Employee>

@{
    ViewData["Title"] = "Index";
    var currnetPage = ViewBag.page;
    var previousPage = currnetPage - 1;
    var naxtPage = currnetPage + 1;
}
<br/>
<h1>Employee</h1>
<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>

<div class="card card-custom">
    <div class="card-header">
        <div class="card-title">
            <span class="card-icon">
                <i class="flaticon2-chat-1 text-primary"></i>
            </span>
            <h3 class="card-label">
                Employee
                <small>Manage All Employees</small>
            </h3>
            <form asp-action="Index" method="get">
                <div class="row">
                    <div class="col-md-6">
                        <input placeholder="Search by name" value="@ViewBag.SearchKey" name="SearchKey" class="form-control" />
                    </div>
                    <div class="" col-md-3"">
                        <select class="form-control" name="GenderType">
                            <option>-- Gender Type --</option>
                            <option value="@Gender.Male">Male</option>
                            <option value="@Gender.Female">Female</option>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <input type="submit" class="btn btn-light-primary font-weight-bold mr-2" value="Search" />
                    </div>
                </div>
            </form>
        </div>
        <div class="card-toolbar">
        </div>
    </div>
    <div class="card-body">
        @if (Model.Any())
        {
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>
                            Id
                        </th>
                        <th>
                            Name
                        </th>
                        <th>
                            Mobile
                        </th>
                        <th>
                            Email
                        </th>
                        <th>
                            Gender
                        </th>
                        <th>
                            Adress
                        </th>
                        <th>
                            Salary
                        </th>
                        <th>
                            Date
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.Id
                            </td>
                            <td>
                                @item.Name
                            </td>
                            <td>
                                @item.Mobile
                            </td>
                            <td>
                                @item.Email
                            </td>
                            <td>
                                @item.Gender
                            </td>
                            <td>
                                @item.Adress
                            </td>
                            <td>
                                @item.Salary
                            </td>
                            <td>
                                @item.CreatedAt.ToString("yyyy/MM/dd")
                            </td>
                            <td>
                                <a href="~/Employee/Edit/@item.Id" class="btn btn-warning"><i class="flaticon flaticon-edit"></i>Edit</a>
                                <a href="~/Employee/Delete/@item.Id" class="btn btn-danger"><i class="flaticon flaticon-delete"></i>Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <div class="d-flex justify-content-between align-items-center flex-wrap">
                <div class="d-flex flex-wrap py-2 mr-3">
                    @if (currnetPage == 1)
                    {
                        <a href="~/Employee/Index?page=@previousPage" class="btn btn-icon btn-light-primary mr-2 my-1 disabled"><i class="ki ki-bold-arrow-back icon-xs"></i></a>
                    }
                    else
                    {
                        <a href="~/Employee/Index?page=@previousPage" class="btn btn-icon btn-light-primary mr-2 my-1"><i class="ki ki-bold-arrow-back icon-xs"></i></a>
                    }


                    @for (int i = 1; i <= ViewBag.numberOFPages; i++)
                    {
                        @if (currnetPage == i)
                        {
                            <a href="~/Employee/Index?page=@i" class="btn btn-icon border-0 btn-hover-primary active mr-2 my-1">@i</a>
                        }
                        else
                        {
                            <a href="~/Employee/Index?page=@i" class="btn btn-icon border-0 btn-hover-primary mr-2 my-1">@i</a>
                        }

                    }

                    @if (currnetPage == ViewBag.numberOFPages)
                    {
                        <a href="~/Employee/Index?page=@naxtPage" class="btn btn-icon btn-light-primary mr-2 my-1 disabled"><i class="ki ki-bold-arrow-next icon-xs"></i></a>
                    }
                    else
                    {
                        <a href="~/Employee/Index?page=@naxtPage" class="btn btn-icon btn-light-primary mr-2 my-1"><i class="ki ki-bold-arrow-next icon-xs"></i></a>
                    }
                </div>
            </div>
        }
        else
        {
            <div class="alert alert-warning">
                No Data in DataBase !!
            </div>
        }
    </div>
</div>

